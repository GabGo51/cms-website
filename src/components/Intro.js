import React from "react";
import { styled } from "styled-components";

const Intro = () => {
  return (
    <Container>
      <h2>La médecine en action</h2>
      <h2>Médecine, santé et performance</h2>
      <h2>Vers l’atteinte de vos objectifs !</h2>
    </Container>
  );
};

const Container = styled.section``;

export default Intro;
